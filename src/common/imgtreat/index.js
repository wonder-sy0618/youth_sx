
import {imageOpen, imageRotate, imageResize} from "./image"
import {textDraw} from "./text"
import create from "./create"

export default {
  imageOpen : imageOpen,
  imageRotate : imageRotate,
  imageResize : imageResize,
  textDraw : textDraw,
  create : create
}
